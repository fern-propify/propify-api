name: api
display-name: Propify API
auth: Headers
auth-schemes:
  HeaderAuth:
    header: propify-api-key
    type: string 
    # header: propify-api-secret
    # type: string
error-discrimination:
  strategy: status-code